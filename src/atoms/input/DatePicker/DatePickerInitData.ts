import { SizeType, StatusType } from './DatePicker.type';

const INIT_STATUS: StatusType = {
  DEFAULT: 'default',
  ERROR: 'error',
};

const INIT_SIZE: SizeType = {
  LARGE: 'large',
  MEDIUM: 'medium',
  SMALL: 'small',
  XSMALL: 'x-small',
};

export { INIT_SIZE, INIT_STATUS };
